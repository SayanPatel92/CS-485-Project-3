[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.0-rc.1"

reqwest = { version = "0.11", features = ["blocking", "json"] }
serde_json = "1.0"
aes = "0.8.2"
block-modes = "0.9.1"
